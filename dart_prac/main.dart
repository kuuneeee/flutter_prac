// 1. Hello World

void main() {
  // main이 entry point -> 함수명을 main에서 바꿔주면 에러가 남
  print('hello world'); 
  // ;을 써줘야 함 -> javascript나 typescript는 ;을 자동으로 달아줌
    // 일부러 ;을 안쓸때도 있기 때문에 수동으로 되어있는 것
    // cascade operator를 종료하는 역할?
}